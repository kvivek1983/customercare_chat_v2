import { ReplaceArrowPipe } from './replace-arrow.pipe';

describe('ReplaceArrowPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceArrowPipe();
    expect(pipe).toBeTruthy();
  });
});
